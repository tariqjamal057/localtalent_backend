export enum USER_TYPE {
    INDIVIDUAL = 1,
    BUSINESS = 2,
}

export enum USER_STATUS {
    ONLINE = 'online',
    OFFLINE = 'offline',
    IN_CALL = 'in_call',
    SEARCHING = 'searching',
    BUSY = 'busy',
}